import client from "@/lib/mongodb";export default async function ProjectsPage(){    const projects = await client.db("mycv").collection("projects").find().toArray();    console.log(projects);    return <>My projects</>}